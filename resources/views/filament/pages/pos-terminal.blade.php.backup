<x-filament-panels::page>
    <div class="h-screen bg-gray-50 dark:bg-gray-900 -m-6" 
         x-data="posTerminal()" 
         x-init="init()">
         
        <!-- Modern Header Bar -->
        <div class="bg-white dark:bg-gray-800 shadow-sm border-b border-gray-200 dark:border-gray-700 px-6 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-4">
                    <div class="flex items-center space-x-2">
                        <div class="w-10 h-10 bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg flex items-center justify-center">
                            <x-heroicon-o-device-tablet class="w-6 h-6 text-white" />
                        </div>
                        <div>
                            <h1 class="text-xl font-bold text-gray-900 dark:text-white">POS Terminal</h1>
                            <p class="text-sm text-gray-500 dark:text-gray-400">{{ \Filament\Facades\Filament::getTenant()?->name ?? 'Wellness Spa' }}</p>
                        </div>
                    </div>
                </div>
                
                <div class="flex items-center space-x-3">
                    <!-- Staff Selector -->
                    <div class="flex items-center space-x-2">
                        <x-heroicon-o-user class="w-5 h-5 text-gray-400" />
                        <select wire:model.live="selectedStaffId" 
                                class="border-0 bg-gray-100 dark:bg-gray-700 rounded-lg text-sm font-medium focus:ring-2 focus:ring-blue-500">
                            <option value="">Select Staff</option>
                            @foreach($this->getStaff() as $staff)
                                <option value="{{ $staff->id }}">{{ $staff->name }}</option>
                            @endforeach
                        </select>
                    </div>
                    
                    <!-- Action Buttons -->
                    <button wire:click="clearCart" 
                            class="bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 px-4 py-2 rounded-lg transition-colors">
                        <x-heroicon-o-arrow-path class="w-4 h-4 mr-2 inline" />
                        New Sale
                    </button>
                    
                    <button wire:click="showDailySummary" 
                            class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg transition-colors">
                        <x-heroicon-o-chart-bar class="w-4 h-4 mr-2 inline" />
                        Daily Summary
                    </button>
                </div>
            </div>
        </div>

        <div class="flex h-full">
            <!-- Left Panel - Product/Service Selection -->
            <div class="flex-1 bg-white dark:bg-gray-800 overflow-hidden">
                <!-- Enhanced Search and Filter Bar -->
                <div class="p-6 border-b-2 border-blue-100 dark:border-blue-800 bg-gradient-to-r from-blue-50 to-purple-50 dark:from-gray-800 dark:to-gray-700">
                    <!-- Header -->
                    <div class="flex items-center justify-between mb-4">
                        <h2 class="text-lg font-bold text-gray-900 dark:text-white">Browse & Select</h2>
                        <div class="text-sm text-gray-600 dark:text-gray-400 bg-white dark:bg-gray-800 px-3 py-1 rounded-lg border">
                            {{ count($this->getServices()) }} services available
                        </div>
                    </div>
                    
                    <div class="flex space-x-4 mb-4">
                        <!-- Enhanced Search Input -->
                        <div class="flex-1 relative">
                            <x-heroicon-o-magnifying-glass class="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-blue-500" />
                            <input wire:model.live.debounce.300ms="searchTerm"
                                   type="text" 
                                   placeholder="Search services by name, category, or description..."
                                   class="w-full pl-12 pr-4 py-3.5 border-2 border-blue-200 dark:border-blue-600 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-800 text-gray-900 dark:text-white font-medium shadow-sm">
                        </div>
                        
                        <!-- Enhanced Category Filter -->
                        <div class="relative">
                            <select wire:model.live="selectedCategory" 
                                    class="appearance-none border-2 border-blue-200 dark:border-blue-600 rounded-xl bg-white dark:bg-gray-800 px-4 py-3.5 pr-10 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-gray-900 dark:text-white font-medium shadow-sm min-w-[160px]">
                                @if($activeTab === 'services')
                                    @foreach($this->getServiceCategories() as $key => $label)
                                        <option value="{{ $key }}">{{ $label }}</option>
                                    @endforeach
                                @else
                                    @foreach($this->getProductCategories() as $key => $label)
                                        <option value="{{ $key }}">{{ $label }}</option>
                                    @endforeach
                                @endif
                            </select>
                            <x-heroicon-o-chevron-down class="absolute right-3 top-1/2 transform -translate-y-1/2 w-5 h-5 text-blue-500 pointer-events-none" />
                        </div>
                    </div>
                    
                    <!-- Enhanced Tab Navigation -->
                    <div class="flex space-x-2 bg-white dark:bg-gray-800 p-1.5 rounded-xl border-2 border-blue-200 dark:border-blue-600 shadow-sm">
                        <button wire:click="$set('activeTab', 'services')" 
                                class="flex-1 py-3 px-4 rounded-lg text-sm font-bold transition-all duration-200 {{ $activeTab === 'services' ? 'bg-gradient-to-r from-blue-500 to-purple-600 text-white shadow-lg transform scale-105' : 'text-gray-600 dark:text-gray-400 hover:bg-blue-50 dark:hover:bg-blue-900/20' }}">
                            <x-heroicon-o-sparkles class="w-5 h-5 mr-2 inline" />
                            Services
                            @if($activeTab === 'services')
                                <span class="ml-2 bg-white/20 text-xs px-2 py-0.5 rounded-full">{{ count($this->getServices()) }}</span>
                            @endif
                        </button>
                        <button wire:click="$set('activeTab', 'products')" 
                                class="flex-1 py-3 px-4 rounded-lg text-sm font-bold transition-all duration-200 {{ $activeTab === 'products' ? 'bg-gradient-to-r from-blue-500 to-purple-600 text-white shadow-lg transform scale-105' : 'text-gray-600 dark:text-gray-400 hover:bg-blue-50 dark:hover:bg-blue-900/20' }}">
                            <x-heroicon-o-cube class="w-5 h-5 mr-2 inline" />
                            Products
                            @if($activeTab === 'products')
                                <span class="ml-2 bg-white/20 text-xs px-2 py-0.5 rounded-full">{{ count($this->getProducts()) }}</span>
                            @endif
                        </button>
                    </div>
                    
                    <!-- Quick Filter Pills -->
                    @if($activeTab === 'services')
                        <div class="mt-4 flex flex-wrap gap-2">
                            <span class="text-xs text-gray-600 dark:text-gray-400 mr-2">Quick filters:</span>
                            <button class="text-xs bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 px-3 py-1 rounded-full hover:bg-blue-200 dark:hover:bg-blue-900/50 transition-colors">
                                Popular
                            </button>
                            <button class="text-xs bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-300 px-3 py-1 rounded-full hover:bg-green-200 dark:hover:bg-green-900/50 transition-colors">
                                Quick (≤30min)
                            </button>
                            <button class="text-xs bg-purple-100 dark:bg-purple-900/30 text-purple-700 dark:text-purple-300 px-3 py-1 rounded-full hover:bg-purple-200 dark:hover:bg-purple-900/50 transition-colors">
                                Premium
                            </button>
                        </div>
                    @endif
                </div>

                <!-- Items Grid -->
                <div class="p-6 overflow-y-auto h-full">
                    @if($activeTab === 'services')
                        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
                            @foreach($this->getServices() as $service)
                                <div wire:click="addToCart('service', {{ $service->id }})" 
                                     class="group bg-white dark:bg-gray-800 rounded-2xl border-2 border-gray-200 dark:border-gray-600 p-5 cursor-pointer hover:shadow-xl hover:border-blue-400 hover:bg-blue-50 dark:hover:bg-blue-900/10 transition-all duration-300 relative overflow-hidden transform hover:-translate-y-1 active:scale-95">
                                    
                                    <!-- Service Icon/Image -->
                                    <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl flex items-center justify-center mb-4 group-hover:shadow-lg group-hover:scale-110 transition-all duration-300">
                                        <x-heroicon-o-sparkles class="w-8 h-8 text-white" />
                                    </div>
                                    
                                    <!-- Add to Cart Badge -->
                                    <div class="absolute top-3 right-3 bg-gradient-to-r from-green-500 to-emerald-600 text-white text-xs px-3 py-1.5 rounded-full font-semibold opacity-0 group-hover:opacity-100 transition-all duration-300 transform translate-y-2 group-hover:translate-y-0 shadow-lg">
                                        <x-heroicon-o-plus class="w-3 h-3 mr-1 inline" />
                                        Quick Book
                                    </div>
                                    
                                    <!-- Service Name -->
                                    <h3 class="font-bold text-gray-900 dark:text-white text-base mb-2 line-clamp-2 group-hover:text-blue-700 dark:group-hover:text-blue-300 transition-colors">{{ $service->name }}</h3>
                                    
                                    <!-- Price and Duration -->
                                    <div class="flex items-center justify-between mb-3">
                                        <span class="text-xl font-bold text-blue-600 dark:text-blue-400">KES {{ number_format($service->price) }}</span>
                                        @if($service->duration_minutes)
                                            <div class="flex items-center bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 text-xs font-semibold px-2.5 py-1.5 rounded-lg">
                                                <x-heroicon-o-clock class="w-3 h-3 mr-1" />
                                                {{ $service->duration_minutes }}min
                                            </div>
                                        @endif
                                    </div>
                                    
                                    <!-- Service Description -->
                                    @if($service->description)
                                        <p class="text-sm text-gray-600 dark:text-gray-400 line-clamp-3 leading-relaxed">{{ $service->description }}</p>
                                    @endif
                                    
                                    <!-- Booking Indicator -->
                                    <div class="mt-4 pt-3 border-t border-gray-100 dark:border-gray-700">
                                        <div class="flex items-center text-xs text-gray-500 dark:text-gray-400">
                                            <x-heroicon-o-calendar-days class="w-3 h-3 mr-1" />
                                            <span>Available for booking</span>
                                        </div>
                                    </div>
                                </div>
                            @endforeach
                        </div>
                    @else
                        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                            @foreach($this->getProducts() as $product)
                                <div wire:click="addToCart('product', {{ $product->id }})" 
                                     class="group bg-white dark:bg-gray-700 rounded-xl border border-gray-200 dark:border-gray-600 p-4 cursor-pointer hover:shadow-lg hover:border-blue-300 transition-all duration-200">
                                    
                                    <!-- Product Icon/Image -->
                                    <div class="w-12 h-12 bg-gradient-to-r from-green-400 to-blue-500 rounded-lg flex items-center justify-center mb-3 group-hover:scale-105 transition-transform">
                                        <x-heroicon-o-cube class="w-6 h-6 text-white" />
                                    </div>
                                    
                                    <h3 class="font-semibold text-gray-900 dark:text-white text-sm mb-1 line-clamp-2">{{ $product->name }}</h3>
                                    
                                    <div class="flex items-center justify-between mb-2">
                                        <span class="text-lg font-bold text-green-600">KES {{ number_format($product->selling_price) }}</span>
                                        <span class="text-xs {{ $product->isLowStock() ? 'text-red-500 bg-red-100' : 'text-gray-500 bg-gray-100' }} dark:bg-gray-600 px-2 py-1 rounded">
                                            {{ $product->current_stock }} {{ $product->unit }}
                                        </span>
                                    </div>
                                    
                                    @if($product->description)
                                        <p class="text-xs text-gray-500 dark:text-gray-400 line-clamp-2">{{ $product->description }}</p>
                                    @endif
                                </div>
                            @endforeach
                        </div>
                    @endif
                </div>
            </div>

            <!-- Right Panel - Cart & Checkout -->
            <div class="w-96 bg-white dark:bg-gray-800 border-l border-gray-200 dark:border-gray-700 flex flex-col">
                <!-- Customer Section -->
                <div class="p-6 border-b border-gray-200 dark:border-gray-700">
                    <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Customer</h3>
                    
                    @if($customerData['type'] === 'walk_in')
                        <div class="space-y-3">
                            <input wire:model.live="customerData.name" 
                                   type="text" 
                                   placeholder="Customer Name (Optional)"
                                   class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 bg-gray-50 dark:bg-gray-700">
                            
                            <input wire:model.live="customerData.phone" 
                                   type="tel" 
                                   placeholder="Phone Number"
                                   class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 bg-gray-50 dark:bg-gray-700">
                        </div>
                    @else
                        <div class="bg-blue-50 dark:bg-blue-900/20 rounded-lg p-3">
                            <p class="font-medium text-blue-900 dark:text-blue-100">{{ $customerData['name'] }}</p>
                            <p class="text-sm text-blue-600 dark:text-blue-300">{{ $customerData['phone'] }}</p>
                            @if($customerLoyaltyPoints > 0)
                                <div class="flex items-center mt-2">
                                    <x-heroicon-o-star class="w-4 h-4 text-yellow-500 mr-1" />
                                    <span class="text-sm text-yellow-600 dark:text-yellow-400">{{ number_format($customerLoyaltyPoints) }} loyalty points</span>
                                </div>
                            @endif
                        </div>
                    @endif
                </div>

                <!-- Phase 2 Discounts Section -->
                <div class="px-6 py-4 border-b border-gray-200 dark:border-gray-700">
                    <div class="grid grid-cols-2 gap-2">
                        <!-- Gift Voucher Button -->
                        <button wire:click="$set('showVoucherModal', true)" 
                                class="flex items-center justify-center p-3 rounded-lg border-2 border-dashed {{ $appliedVoucherCode ? 'border-green-300 bg-green-50 text-green-700' : 'border-gray-300 hover:border-gray-400' }} transition-colors">
                            <x-heroicon-o-gift class="w-5 h-5 mr-2" />
                            <span class="text-sm font-medium">{{ $appliedVoucherCode ? 'Voucher Applied' : 'Gift Voucher' }}</span>
                        </button>

                        <!-- Discount Coupon Button -->
                        <button wire:click="$set('showCouponModal', true)" 
                                class="flex items-center justify-center p-3 rounded-lg border-2 border-dashed {{ $appliedCouponCode ? 'border-orange-300 bg-orange-50 text-orange-700' : 'border-gray-300 hover:border-gray-400' }} transition-colors">
                            <x-heroicon-o-ticket class="w-5 h-5 mr-2" />
                            <span class="text-sm font-medium">{{ $appliedCouponCode ? 'Coupon Applied' : 'Coupon' }}</span>
                        </button>

                        <!-- Loyalty Points Button -->
                        @if($customerData['client_id'] && $customerLoyaltyPoints > 0)
                            <button wire:click="$set('showLoyaltyModal', true)" 
                                    class="flex items-center justify-center p-3 rounded-lg border-2 border-dashed {{ $loyaltyPointsToUse > 0 ? 'border-yellow-300 bg-yellow-50 text-yellow-700' : 'border-gray-300 hover:border-gray-400' }} transition-colors col-span-2">
                                <x-heroicon-o-star class="w-5 h-5 mr-2" />
                                <span class="text-sm font-medium">{{ $loyaltyPointsToUse > 0 ? $loyaltyPointsToUse . ' Points Used' : 'Use Loyalty Points' }}</span>
                            </button>
                        @endif
                    </div>

                    <!-- Applied Promotions Display -->
                    @if(count($appliedPromotions) > 0)
                        <div class="mt-3 space-y-2">
                            <h4 class="text-sm font-medium text-gray-700 dark:text-gray-300">Active Promotions</h4>
                            @foreach($appliedPromotions as $promotion)
                                <div class="flex items-center justify-between p-2 bg-green-50 dark:bg-green-900/20 rounded-lg">
                                    <span class="text-sm text-green-800 dark:text-green-200">{{ $promotion['name'] }}</span>
                                    <span class="text-sm font-medium text-green-600 dark:text-green-400">{{ $promotion['formatted_discount_value'] }}</span>
                                </div>
                            @endforeach
                        </div>
                    @endif
                </div>

                <!-- Enhanced Cart Items -->
                <div class="flex-1 overflow-y-auto p-6">
                    <!-- Cart Header -->
                    <div class="flex items-center justify-between mb-6">
                        <div class="flex items-center space-x-3">
                            <div class="w-10 h-10 bg-blue-100 dark:bg-blue-900 rounded-full flex items-center justify-center">
                                <x-heroicon-o-shopping-cart class="w-5 h-5 text-blue-600 dark:text-blue-400" />
                            </div>
                            <div>
                                <h3 class="text-xl font-bold text-gray-900 dark:text-white">Shopping Cart</h3>
                                <p class="text-sm text-gray-500 dark:text-gray-400">Review your items</p>
                            </div>
                        </div>
                        @if(!empty($cart))
                            <div class="flex items-center space-x-2">
                                <span class="bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200 text-sm font-bold px-3 py-1 rounded-full">
                                    {{ count($cart) }} {{ count($cart) === 1 ? 'item' : 'items' }}
                                </span>
                                <button wire:click="clearCart" 
                                        class="text-red-500 hover:text-red-700 text-sm font-medium px-3 py-1 rounded-lg hover:bg-red-50 dark:hover:bg-red-900/20 transition-colors"
                                        onclick="return confirm('Are you sure you want to clear the entire cart?')">
                                    Clear All
                                </button>
                            </div>
                        @endif
                    </div>

                    @if(empty($cart))
                        <!-- Empty Cart State -->
                        <div class="text-center py-12 bg-gray-50 dark:bg-gray-800 rounded-2xl border-2 border-dashed border-gray-300 dark:border-gray-600">
                            <x-heroicon-o-shopping-cart class="w-16 h-16 text-gray-400 mx-auto mb-4" />
                            <h4 class="text-lg font-semibold text-gray-700 dark:text-gray-300 mb-2">Your cart is empty</h4>
                            <p class="text-gray-500 dark:text-gray-400 mb-4">Add services or products to get started</p>
                            <div class="flex justify-center space-x-2">
                                <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                    <x-heroicon-o-sparkles class="w-3 h-3 mr-1" />
                                    Services
                                </span>
                                <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                    <x-heroicon-o-cube class="w-3 h-3 mr-1" />
                                    Products
                                </span>
                            </div>
                        </div>
                    @else
                        <!-- Cart Items List -->
                        <div class="space-y-4">
                            @foreach($cart as $cartItemId => $item)
                                <div class="bg-white dark:bg-gray-700 rounded-2xl border-2 border-gray-200 dark:border-gray-600 shadow-sm hover:shadow-md transition-all duration-200">
                                    <!-- Item Header -->
                                    <div class="p-4 border-b border-gray-200 dark:border-gray-600">
                                        <div class="flex items-start justify-between">
                                            <div class="flex items-center space-x-3 flex-1">
                                                <!-- Service/Product Icon -->
                                                <div class="w-12 h-12 rounded-full flex items-center justify-center {{ $item['type'] === 'service' ? 'bg-blue-100 dark:bg-blue-900' : 'bg-green-100 dark:bg-green-900' }}">
                                                    @if($item['type'] === 'service')
                                                        <x-heroicon-o-sparkles class="w-6 h-6 text-blue-600 dark:text-blue-400" />
                                                    @else
                                                        <x-heroicon-o-cube class="w-6 h-6 text-green-600 dark:text-green-400" />
                                                    @endif
                                                </div>
                                                
                                                <div class="flex-1">
                                                    <div class="flex items-center space-x-2 mb-1">
                                                        <h4 class="font-bold text-gray-900 dark:text-white">{{ $item['name'] }}</h4>
                                                        <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium {{ $item['type'] === 'service' ? 'bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200' : 'bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200' }}">
                                                            {{ ucfirst($item['type']) }}
                                                        </span>
                                                    </div>
                                                    
                                                    @if($item['type'] === 'service' && $item['duration'])
                                                        <div class="flex items-center space-x-1 text-sm text-gray-600 dark:text-gray-400">
                                                            <x-heroicon-o-clock class="w-4 h-4" />
                                                            <span>{{ $item['duration'] }} minutes</span>
                                                        </div>
                                                    @endif
                                                </div>
                                            </div>
                                            
                                            <!-- Remove Button -->
                                            <button wire:click="removeFromCart('{{ $cartItemId }}')" 
                                                    class="text-gray-400 hover:text-red-500 p-2 rounded-lg hover:bg-red-50 dark:hover:bg-red-900/20 transition-all duration-200"
                                                    onclick="return confirm('Remove this item from cart?')">
                                                <x-heroicon-o-trash class="w-5 h-5" />
                                            </button>
                                        </div>
                                    </div>
                                    
                                    <!-- Service Booking Details -->
                                    @if($item['type'] === 'service')
                                        <div class="p-4 bg-blue-50 dark:bg-blue-900/20">
                                            <h5 class="font-semibold text-blue-800 dark:text-blue-200 mb-3 flex items-center">
                                                <x-heroicon-o-calendar class="w-4 h-4 mr-2" />
                                                Booking Details
                                            </h5>
                                            
                                            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                                                <!-- Staff Assignment -->
                                                <div class="flex items-center space-x-2">
                                                    <x-heroicon-o-user class="w-4 h-4 text-gray-500" />
                                                    <span class="text-sm text-gray-700 dark:text-gray-300">
                                                        <strong>Staff:</strong> {{ $item['staff_name'] ?? 'No preference' }}
                                                    </span>
                                                </div>
                                                
                                                <!-- Date & Time -->
                                                @if(isset($item['appointment_date']) && isset($item['appointment_time']))
                                                    <div class="flex items-center space-x-2">
                                                        <x-heroicon-o-calendar-days class="w-4 h-4 text-gray-500" />
                                                        <span class="text-sm text-gray-700 dark:text-gray-300">
                                                            <strong>When:</strong> {{ date('M j, Y \a\t g:i A', strtotime($item['appointment_date'] . ' ' . $item['appointment_time'])) }}
                                                        </span>
                                                    </div>
                                                @endif
                                            </div>
                                            
                                            <!-- Booking Status & Actions -->
                                            <div class="mt-3 flex items-center justify-between">
                                                @if($this->isServiceBookingComplete($cartItemId))
                                                    <div class="flex items-center space-x-2">
                                                        <x-heroicon-o-check-circle class="w-5 h-5 text-green-500" />
                                                        <span class="text-green-600 dark:text-green-400 font-medium">Booking Complete</span>
                                                    </div>
                                                @else
                                                    <div class="flex items-center space-x-2">
                                                        <x-heroicon-o-exclamation-triangle class="w-5 h-5 text-orange-500" />
                                                        <span class="text-orange-600 dark:text-orange-400 font-medium">Booking Incomplete</span>
                                                    </div>
                                                @endif
                                                
                                                <button wire:click="startServiceBooking('{{ $cartItemId }}')" 
                                                        class="inline-flex items-center px-3 py-1 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-lg transition-colors">
                                                    <x-heroicon-o-pencil class="w-4 h-4 mr-1" />
                                                    {{ $this->isServiceBookingComplete($cartItemId) ? 'Edit Booking' : 'Complete Booking' }}
                                                </button>
                                            </div>
                                        </div>
                                    @endif
                                    
                                    <!-- Quantity & Price Controls -->
                                    <div class="p-4 flex items-center justify-between">
                                        <!-- Quantity Controls -->
                                        @if($item['type'] === 'product')
                                            <div class="flex items-center space-x-3">
                                                <span class="text-sm font-medium text-gray-700 dark:text-gray-300">Quantity:</span>
                                                <div class="flex items-center space-x-2">
                                                    <button wire:click="updateQuantity('{{ $cartItemId }}', {{ $item['quantity'] - 1 }})" 
                                                            class="w-8 h-8 bg-gray-200 dark:bg-gray-600 rounded-full flex items-center justify-center text-gray-600 dark:text-gray-300 hover:bg-gray-300 dark:hover:bg-gray-500 transition-colors"
                                                            {{ $item['quantity'] <= 1 ? 'disabled' : '' }}>
                                                        <x-heroicon-o-minus class="w-4 h-4" />
                                                    </button>
                                                    
                                                    <span class="font-bold text-lg w-8 text-center">{{ $item['quantity'] }}</span>
                                                    
                                                    <button wire:click="updateQuantity('{{ $cartItemId }}', {{ $item['quantity'] + 1 }})" 
                                                            class="w-8 h-8 bg-gray-200 dark:bg-gray-600 rounded-full flex items-center justify-center text-gray-600 dark:text-gray-300 hover:bg-gray-300 dark:hover:bg-gray-500 transition-colors">
                                                        <x-heroicon-o-plus class="w-4 h-4" />
                                                    </button>
                                                </div>
                                                @if(isset($item['stock_available']))
                                                    <span class="text-xs text-gray-500">({{ $item['stock_available'] }} in stock)</span>
                                                @endif
                                            </div>
                                        @else
                                            <div class="flex items-center space-x-2 text-sm text-gray-600 dark:text-gray-400">
                                                <x-heroicon-o-information-circle class="w-4 h-4" />
                                                <span>Single service appointment</span>
                                            </div>
                                        @endif
                                        
                                        <!-- Price Display -->
                                        <div class="text-right">
                                            @if($item['quantity'] > 1)
                                                <div class="text-sm text-gray-500 dark:text-gray-400">
                                                    KES {{ number_format($item['price']) }} × {{ $item['quantity'] }}
                                                </div>
                                            @endif
                                            <div class="text-xl font-bold text-gray-900 dark:text-white">
                                                KES {{ number_format($item['price'] * $item['quantity']) }}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            @endforeach
                        </div>
                    @endif
                </div>

                <!-- Simple Checkout Button (when not in checkout flow) -->
                @if(!empty($cart) && !$showCheckoutFlow)
                    <div class="border-t border-gray-200 dark:border-gray-700 p-6">
                        <!-- Quick Summary -->
                        <div class="mb-4">
                            <div class="flex justify-between items-center mb-2">
                                <span class="text-lg font-semibold text-gray-900 dark:text-white">Total</span>
                                <span class="text-2xl font-bold text-blue-600 dark:text-blue-400">
                                    KES {{ number_format($subtotal * 1.16) }}
                                </span>
                            </div>
                            <p class="text-sm text-gray-500 dark:text-gray-400">{{ count($cart) }} item(s) • Tax included</p>
                        </div>
                        
                        <!-- Checkout Button -->
                        <button wire:click="startCheckout" 
                                class="w-full bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-bold py-4 px-6 rounded-xl transition-all duration-200 transform hover:scale-105 shadow-lg hover:shadow-xl">
                            <div class="flex items-center justify-center space-x-2">
                                <x-heroicon-o-shopping-cart class="w-5 h-5" />
                                <span class="text-lg">Proceed to Checkout</span>
                                <x-heroicon-o-arrow-right class="w-5 h-5" />
                            </div>
                        </button>
                    </div>
                @endif
            </div>
        </div>

        <!-- Multi-Step Checkout Flow -->
        @if($showCheckoutFlow)
        <div x-data="{ show: @entangle('showCheckoutFlow') }"
             x-show="show" 
             x-cloak
             x-transition:enter="transition ease-out duration-300"
             x-transition:enter-start="opacity-0 scale-95"
             x-transition:enter-end="opacity-100 scale-100"
             x-transition:leave="transition ease-in duration-200"
             x-transition:leave-start="opacity-100 scale-100"
             x-transition:leave-end="opacity-0 scale-95"
             class="fixed inset-0 bg-black bg-opacity-60 backdrop-blur-sm flex items-center justify-center p-4 z-50">
            
            <div class="bg-white dark:bg-gray-800 rounded-3xl shadow-2xl border border-gray-200 dark:border-gray-700 w-full max-w-5xl max-h-[95vh] overflow-hidden flex flex-col">
                <!-- Checkout Header -->
                <div class="bg-gradient-to-r from-blue-600 via-purple-600 to-blue-700 text-white p-6 relative overflow-hidden">
                    <div class="absolute inset-0 bg-gradient-to-r from-blue-500/20 to-purple-500/20"></div>
                    
                    <div class="relative flex items-center justify-between">
                        <div class="flex items-center space-x-4">
                            <div class="w-12 h-12 bg-white/20 backdrop-blur-sm rounded-xl flex items-center justify-center border border-white/30">
                                <x-heroicon-o-shopping-cart class="w-6 h-6 text-white" />
                            </div>
                            <div>
                                <h3 class="text-2xl font-bold mb-1">Checkout Process</h3>
                                <p class="text-blue-100">Step {{ $checkoutStep === 'cart_review' ? '1' : ($checkoutStep === 'payment' ? '2' : '3') }} of 3</p>
                            </div>
                        </div>
                        <button wire:click="cancelCheckout" 
                                class="text-white/80 hover:text-white hover:bg-white/20 rounded-xl p-3 transition-all duration-200">
                            <x-heroicon-o-x-mark class="w-6 h-6" />
                        </button>
                    </div>
                    
                    <!-- Progress Steps -->
                    <div class="mt-6 bg-white/10 backdrop-blur-sm rounded-xl p-4 border border-white/20">
                        <div class="flex items-center justify-between">
                            <!-- Step 1: Cart Review -->
                            <div class="flex flex-col items-center flex-1">
                                <div class="w-10 h-10 {{ $checkoutStep === 'cart_review' ? 'bg-white text-blue-600 ring-4 ring-white/30' : ($checkoutStep === 'payment' || $checkoutStep === 'confirmation' ? 'bg-green-500 text-white' : 'bg-white/20 text-white border-2 border-white/40') }} rounded-full flex items-center justify-center text-sm font-bold shadow-lg transition-all duration-300">
                                    @if($checkoutStep === 'payment' || $checkoutStep === 'confirmation')
                                        <x-heroicon-o-check class="w-5 h-5" />
                                    @else
                                        <x-heroicon-o-shopping-cart class="w-5 h-5" />
                                    @endif
                                </div>
                                <span class="text-xs font-semibold mt-2 {{ $checkoutStep === 'cart_review' ? 'text-white' : 'text-blue-200' }}">Review Cart</span>
                            </div>
                            
                            <div class="flex-1 h-1 mx-3 {{ $checkoutStep === 'payment' || $checkoutStep === 'confirmation' ? 'bg-green-400' : 'bg-white/30' }} rounded-full transition-all duration-500"></div>
                            
                            <!-- Step 2: Payment -->
                            <div class="flex flex-col items-center flex-1">
                                <div class="w-10 h-10 {{ $checkoutStep === 'payment' ? 'bg-white text-blue-600 ring-4 ring-white/30' : ($checkoutStep === 'confirmation' ? 'bg-green-500 text-white' : 'bg-white/20 text-white border-2 border-white/40') }} rounded-full flex items-center justify-center text-sm font-bold shadow-lg transition-all duration-300">
                                    @if($checkoutStep === 'confirmation')
                                        <x-heroicon-o-check class="w-5 h-5" />
                                    @else
                                        <x-heroicon-o-credit-card class="w-5 h-5" />
                                    @endif
                                </div>
                                <span class="text-xs font-semibold mt-2 {{ $checkoutStep === 'payment' ? 'text-white' : 'text-blue-200' }}">Payment</span>
                            </div>
                            
                            <div class="flex-1 h-1 mx-3 {{ $checkoutStep === 'confirmation' ? 'bg-green-400' : 'bg-white/30' }} rounded-full transition-all duration-500"></div>
                            
                            <!-- Step 3: Confirmation -->
                            <div class="flex flex-col items-center flex-1">
                                <div class="w-10 h-10 {{ $checkoutStep === 'confirmation' ? 'bg-white text-blue-600 ring-4 ring-white/30' : 'bg-white/20 text-white border-2 border-white/40' }} rounded-full flex items-center justify-center text-sm font-bold shadow-lg transition-all duration-300">
                                    <x-heroicon-o-check-circle class="w-5 h-5" />
                                </div>
                                <span class="text-xs font-semibold mt-2 {{ $checkoutStep === 'confirmation' ? 'text-white' : 'text-blue-200' }}">Confirm</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Dynamic Checkout Content -->
                <div class="flex-1 overflow-y-auto p-8">
                    @if($checkoutStep === 'cart_review')
                        @include('filament.pages.checkout.cart-review')
                    @elseif($checkoutStep === 'payment')
                        @include('filament.pages.checkout.payment-method')
                    @elseif($checkoutStep === 'confirmation')
                        @include('filament.pages.checkout.confirmation')
                    @endif
                </div>
                
                <!-- Checkout Navigation Footer -->
                <div class="bg-gray-50 dark:bg-gray-800 border-t border-gray-200 dark:border-gray-700 p-6">
                    <div class="flex justify-between items-center">
                        @if($checkoutStep === 'cart_review')
                            <button wire:click="cancelCheckout" 
                                    class="flex items-center space-x-2 px-6 py-3 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 font-semibold rounded-xl transition-colors">
                                <x-heroicon-o-x-mark class="w-5 h-5" />
                                <span>Cancel</span>
                            </button>
                            <button wire:click="proceedToCheckoutPayment" 
                                    class="flex items-center space-x-2 px-8 py-3 bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-bold rounded-xl transition-all duration-200 transform hover:scale-105 shadow-lg">
                                <span>Continue to Payment</span>
                                <x-heroicon-o-arrow-right class="w-5 h-5" />
                            </button>
                        @elseif($checkoutStep === 'payment')
                            <button wire:click="goBackInCheckout" 
                                    class="flex items-center space-x-2 px-6 py-3 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 font-semibold rounded-xl transition-colors">
                                <x-heroicon-o-arrow-left class="w-5 h-5" />
                                <span>Back to Cart</span>
                            </button>
                            <button wire:click="proceedToConfirmation" 
                                    class="flex items-center space-x-2 px-8 py-3 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-bold rounded-xl transition-all duration-200 transform hover:scale-105 shadow-lg">
                                <span>Review Order</span>
                                <x-heroicon-o-arrow-right class="w-5 h-5" />
                            </button>
                        @elseif($checkoutStep === 'confirmation')
                            <button wire:click="goBackInCheckout" 
                                    class="flex items-center space-x-2 px-6 py-3 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 font-semibold rounded-xl transition-colors">
                                <x-heroicon-o-arrow-left class="w-5 h-5" />
                                <span>Back to Payment</span>
                            </button>
                            <button wire:click="completeTransaction" 
                                    class="flex items-center space-x-2 px-8 py-3 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-bold rounded-xl transition-all duration-200 transform hover:scale-105 shadow-lg">
                                <x-heroicon-o-check class="w-5 h-5" />
                                <span>Complete Purchase</span>
                            </button>
                        @endif
                    </div>
                </div>
            </div>
        </div>
        @endif

        <!-- Enhanced Totals & Payment (old section, will be removed) -->
        @if(!empty($cart) && false)
                    <div class="border-t border-gray-200 dark:border-gray-700 p-6">
                        <!-- Enhanced Totals -->
                        <div class="space-y-2 mb-4">
                            <div class="flex justify-between text-sm">
                                <span class="text-gray-600 dark:text-gray-400">Subtotal</span>
                                <span class="font-medium">KES {{ number_format($subtotal, 2) }}</span>
                            </div>
                            
                            <!-- Manual Discount Input -->
                            <div class="flex justify-between items-center">
                                <span class="text-gray-600 dark:text-gray-400 text-sm">Manual Discount</span>
                                <input wire:model.live="discountAmount" 
                                       type="number" 
                                       step="0.01" 
                                       min="0"
                                       placeholder="0.00"
                                       class="w-20 text-right text-sm border border-gray-300 dark:border-gray-600 rounded px-2 py-1 bg-gray-50 dark:bg-gray-700">
                            </div>

                            <!-- Phase 2 Discounts Display -->
                            @if($voucherDiscountAmount > 0)
                                <div class="flex justify-between text-sm text-green-600">
                                    <span>Gift Voucher</span>
                                    <span>-KES {{ number_format($voucherDiscountAmount, 2) }}</span>
                                </div>
                            @endif

                            @if($couponDiscountAmount > 0)
                                <div class="flex justify-between text-sm text-orange-600">
                                    <span>Coupon Discount</span>
                                    <span>-KES {{ number_format($couponDiscountAmount, 2) }}</span>
                                </div>
                            @endif

                            @if($loyaltyDiscountAmount > 0)
                                <div class="flex justify-between text-sm text-yellow-600">
                                    <span>Loyalty Points ({{ $loyaltyPointsToUse }} pts)</span>
                                    <span>-KES {{ number_format($loyaltyDiscountAmount, 2) }}</span>
                                </div>
                            @endif
                            
                            <!-- Tip Input -->
                            <div class="flex justify-between items-center">
                                <span class="text-gray-600 dark:text-gray-400 text-sm">Tip</span>
                                <input wire:model.live="tipAmount" 
                                       type="number" 
                                       step="0.01" 
                                       min="0"
                                       placeholder="0.00"
                                       class="w-20 text-right text-sm border border-gray-300 dark:border-gray-600 rounded px-2 py-1 bg-gray-50 dark:bg-gray-700">
                            </div>
                            
                            <div class="flex justify-between text-sm">
                                <span class="text-gray-600 dark:text-gray-400">Tax (16%)</span>
                                <span class="font-medium">KES {{ number_format($taxAmount, 2) }}</span>
                            </div>
                            
                            <div class="flex justify-between text-lg font-bold border-t pt-2">
                                <span>Total</span>
                                <span class="text-blue-600">KES {{ number_format($totalAmount, 2) }}</span>
                            </div>
                        </div>

                        <!-- Enhanced Payment Method Selection -->
                        <div class="mb-6">
                            <label class="block text-lg font-bold text-gray-800 dark:text-gray-200 mb-4">Choose Payment Method</label>
                            <div class="grid grid-cols-1 gap-3">
                                <!-- Cash Payment -->
                                <button wire:click="$set('paymentMethod', 'cash')" 
                                        class="group p-4 rounded-xl border-2 transition-all duration-200 {{ $paymentMethod === 'cash' ? 'border-green-500 bg-green-50 dark:bg-green-900/20 text-green-700 dark:text-green-300 shadow-lg' : 'border-gray-300 dark:border-gray-600 hover:border-green-400 hover:bg-green-50 dark:hover:bg-green-900/10' }}">
                                    <div class="flex items-center justify-between">
                                        <div class="flex items-center">
                                            <div class="w-10 h-10 bg-green-500 rounded-lg flex items-center justify-center mr-3 group-hover:shadow-md transition-shadow">
                                                <x-heroicon-o-banknotes class="w-6 h-6 text-white" />
                                            </div>
                                            <div class="text-left">
                                                <p class="font-semibold text-gray-900 dark:text-white">Cash Payment</p>
                                                <p class="text-sm text-gray-600 dark:text-gray-400">Pay with physical cash</p>
                                            </div>
                                        </div>
                                        @if($paymentMethod === 'cash')
                                            <x-heroicon-o-check-circle class="w-6 h-6 text-green-600" />
                                        @endif
                                    </div>
                                </button>
                                
                                <!-- M-Pesa Payment -->
                                <button wire:click="$set('paymentMethod', 'mpesa')" 
                                        class="group p-4 rounded-xl border-2 transition-all duration-200 {{ $paymentMethod === 'mpesa' ? 'border-orange-500 bg-orange-50 dark:bg-orange-900/20 text-orange-700 dark:text-orange-300 shadow-lg' : 'border-gray-300 dark:border-gray-600 hover:border-orange-400 hover:bg-orange-50 dark:hover:bg-orange-900/10' }}">
                                    <div class="flex items-center justify-between">
                                        <div class="flex items-center">
                                            <div class="w-10 h-10 bg-orange-500 rounded-lg flex items-center justify-center mr-3 group-hover:shadow-md transition-shadow">
                                                <x-heroicon-o-device-phone-mobile class="w-6 h-6 text-white" />
                                            </div>
                                            <div class="text-left">
                                                <p class="font-semibold text-gray-900 dark:text-white">M-Pesa</p>
                                                <p class="text-sm text-gray-600 dark:text-gray-400">Pay via mobile money</p>
                                            </div>
                                        </div>
                                        @if($paymentMethod === 'mpesa')
                                            <x-heroicon-o-check-circle class="w-6 h-6 text-orange-600" />
                                        @endif
                                    </div>
                                </button>
                                
                                <!-- Card Payment -->
                                <button wire:click="$set('paymentMethod', 'card')" 
                                        class="group p-4 rounded-xl border-2 transition-all duration-200 {{ $paymentMethod === 'card' ? 'border-blue-500 bg-blue-50 dark:bg-blue-900/20 text-blue-700 dark:text-blue-300 shadow-lg' : 'border-gray-300 dark:border-gray-600 hover:border-blue-400 hover:bg-blue-50 dark:hover:bg-blue-900/10' }}">
                                    <div class="flex items-center justify-between">
                                        <div class="flex items-center">
                                            <div class="w-10 h-10 bg-blue-500 rounded-lg flex items-center justify-center mr-3 group-hover:shadow-md transition-shadow">
                                                <x-heroicon-o-credit-card class="w-6 h-6 text-white" />
                                            </div>
                                            <div class="text-left">
                                                <p class="font-semibold text-gray-900 dark:text-white">Card Payment</p>
                                                <p class="text-sm text-gray-600 dark:text-gray-400">Debit/Credit card</p>
                                            </div>
                                        </div>
                                        @if($paymentMethod === 'card')
                                            <x-heroicon-o-check-circle class="w-6 h-6 text-blue-600" />
                                        @endif
                                    </div>
                                </button>
                                
                                <!-- Split Payment -->
                                <button wire:click="initiateSplitPayment" 
                                        class="group p-4 rounded-xl border-2 border-dashed transition-all duration-200 {{ $paymentMethod === 'mixed' ? 'border-purple-500 bg-purple-50 dark:bg-purple-900/20 text-purple-700 dark:text-purple-300 shadow-lg' : 'border-gray-300 dark:border-gray-600 hover:border-purple-400 hover:bg-purple-50 dark:hover:bg-purple-900/10' }}">
                                    <div class="flex items-center justify-between">
                                        <div class="flex items-center">
                                            <div class="w-10 h-10 bg-purple-500 rounded-lg flex items-center justify-center mr-3 group-hover:shadow-md transition-shadow">
                                                <x-heroicon-o-banknotes class="w-4 h-4 text-white" />
                                                <x-heroicon-o-credit-card class="w-4 h-4 text-white -ml-2" />
                                            </div>
                                            <div class="text-left">
                                                <p class="font-semibold text-gray-900 dark:text-white">Split Payment</p>
                                                <p class="text-sm text-gray-600 dark:text-gray-400">Combine multiple payment methods</p>
                                            </div>
                                        </div>
                                        @if($paymentMethod === 'mixed')
                                            <x-heroicon-o-check-circle class="w-6 h-6 text-purple-600" />
                                        @endif
                                    </div>
                                </button>
                            </div>
                            
                            <!-- Payment Method Specific Fields -->
                            @if($paymentMethod === 'mpesa')
                                <div class="mt-4 p-4 bg-orange-50 dark:bg-orange-900/20 border-2 border-orange-200 dark:border-orange-700 rounded-xl">
                                    <label class="block text-sm font-semibold text-orange-800 dark:text-orange-200 mb-2">M-Pesa Phone Number</label>
                                    <input wire:model="mpesaPhone" 
                                           type="tel" 
                                           placeholder="254XXXXXXXXX"
                                           class="w-full px-4 py-3 border-2 border-orange-300 dark:border-orange-600 rounded-lg focus:ring-2 focus:ring-orange-500 focus:border-orange-500 bg-white dark:bg-gray-800">
                                </div>
                            @elseif($paymentMethod === 'card')
                                <div class="mt-4 p-4 bg-blue-50 dark:bg-blue-900/20 border-2 border-blue-200 dark:border-blue-700 rounded-xl">
                                    <p class="text-sm font-semibold text-blue-800 dark:text-blue-200 mb-2">Card Terminal Ready</p>
                                    <p class="text-xs text-blue-600 dark:text-blue-400">Please insert or swipe the card on the terminal</p>
                                </div>
                            @endif
                        </div>

                        <!-- Enhanced Process Payment Button -->
                        <div class="space-y-3">
                            <!-- Payment Summary -->
                            <div class="p-4 bg-gradient-to-r from-gray-50 to-blue-50 dark:from-gray-800 dark:to-blue-900 border-2 border-blue-200 dark:border-blue-600 rounded-xl">
                                <div class="flex justify-between items-center">
                                    <span class="text-sm font-medium text-gray-700 dark:text-gray-300">Amount to Charge:</span>
                                    <span class="text-xl font-bold text-blue-600 dark:text-blue-400">KES {{ number_format($totalAmount, 2) }}</span>
                                </div>
                                @if($paymentMethod === 'mpesa' && !empty($mpesaPhone))
                                    <div class="flex justify-between items-center mt-2">
                                        <span class="text-xs text-gray-600 dark:text-gray-400">M-Pesa Number:</span>
                                        <span class="text-sm font-semibold text-orange-600 dark:text-orange-400">{{ $mpesaPhone }}</span>
                                    </div>
                                @endif
                            </div>
                            
                            <!-- Main Payment Button -->
                            <button wire:click="processPayment" 
                                    wire:loading.attr="disabled"
                                    wire:target="processPayment"
                                    @if($paymentMethod === 'mpesa' && empty($mpesaPhone)) disabled @endif
                                    class="w-full bg-gradient-to-r from-green-500 to-emerald-600 hover:from-green-600 hover:to-emerald-700 text-white font-bold py-5 px-6 rounded-xl border-2 border-green-400 hover:border-green-500 shadow-lg hover:shadow-xl transition-all duration-200 transform hover:scale-105 disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none focus:outline-none focus:ring-4 focus:ring-green-300">
                                
                                <span wire:loading.remove wire:target="processPayment" class="flex items-center justify-center">
                                    @if($paymentMethod === 'cash')
                                        <x-heroicon-o-banknotes class="w-6 h-6 mr-3" />
                                        Complete Cash Payment
                                    @elseif($paymentMethod === 'mpesa')
                                        <x-heroicon-o-device-phone-mobile class="w-6 h-6 mr-3" />
                                        Send M-Pesa Request
                                    @elseif($paymentMethod === 'card')
                                        <x-heroicon-o-credit-card class="w-6 h-6 mr-3" />
                                        Process Card Payment
                                    @else
                                        <x-heroicon-o-credit-card class="w-6 h-6 mr-3" />
                                        Process Payment
                                    @endif
                                    <span class="text-lg">- KES {{ number_format($totalAmount, 2) }}</span>
                                </span>
                                
                                <span wire:loading wire:target="processPayment" class="flex items-center justify-center">
                                    <svg class="animate-spin -ml-1 mr-3 h-6 w-6 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                                    </svg>
                                    <span class="text-lg font-semibold">
                                        @if($paymentMethod === 'mpesa')
                                            Sending M-Pesa Request...
                                        @else
                                            Processing Payment...
                                        @endif
                                    </span>
                                </span>
                            </button>
                            
                            <!-- Quick Actions -->
                            <div class="flex space-x-2">
                                <button class="flex-1 py-2 px-3 bg-gray-200 dark:bg-gray-700 hover:bg-gray-300 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 font-medium text-sm rounded-lg transition-colors">
                                    <x-heroicon-o-printer class="w-4 h-4 mr-1 inline" />
                                    Print Receipt
                                </button>
                                <button class="flex-1 py-2 px-3 bg-blue-200 dark:bg-blue-700 hover:bg-blue-300 dark:hover:bg-blue-600 text-blue-700 dark:text-blue-300 font-medium text-sm rounded-lg transition-colors">
                                    <x-heroicon-o-envelope class="w-4 h-4 mr-1 inline" />
                                    Email Receipt
                                </button>
                            </div>
                        </div>
                    </div>
                @endif
            </div>
        </div>

        <!-- Gift Voucher Modal -->
        @if($showVoucherModal)
        <div x-data="{ show: @entangle('showVoucherModal') }"
             x-show="show" 
             x-cloak
             x-transition:enter="transition ease-out duration-300"
             x-transition:enter-start="opacity-0 scale-95"
             x-transition:enter-end="opacity-100 scale-100"
             x-transition:leave="transition ease-in duration-200"
             x-transition:leave-start="opacity-100 scale-100"
             x-transition:leave-end="opacity-0 scale-95"
             class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50">
            
            <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl w-full max-w-md">
                <!-- Modal Header -->
                <div class="bg-gradient-to-r from-green-500 to-emerald-600 text-white p-6 rounded-t-2xl">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-3">
                            <div class="w-10 h-10 bg-white bg-opacity-20 rounded-full flex items-center justify-center">
                                <x-heroicon-o-gift class="w-6 h-6" />
                            </div>
                            <div>
                                <h3 class="text-xl font-bold">Gift Voucher</h3>
                                <p class="text-green-100 text-sm">Apply your voucher code</p>
                            </div>
                        </div>
                        <button wire:click="$set('showVoucherModal', false)" 
                                class="text-white hover:bg-white hover:bg-opacity-20 rounded-lg p-2 transition-colors">
                            <x-heroicon-o-x-mark class="w-5 h-5" />
                        </button>
                    </div>
                </div>
                
                <!-- Modal Content -->
                <div class="p-6 space-y-6">
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-3">Enter Voucher Code</label>
                        <div class="relative">
                            <input wire:model="appliedVoucherCode" 
                                   type="text" 
                                   placeholder="e.g., GIFT2024ABC"
                                   class="w-full px-4 py-3 pl-12 border-2 border-gray-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-transparent bg-gray-50 dark:bg-gray-700 text-lg font-mono uppercase">
                            <x-heroicon-o-ticket class="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
                        </div>
                    </div>
                    
                    @if($appliedVoucherCode)
                        <div class="bg-gradient-to-r from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 p-4 rounded-xl border border-green-200 dark:border-green-800">
                            <div class="flex items-center space-x-2 mb-2">
                                <x-heroicon-o-check-circle class="w-5 h-5 text-green-600" />
                                <p class="text-sm font-medium text-green-800 dark:text-green-200">
                                    Voucher: <span class="font-mono">{{ $appliedVoucherCode }}</span>
                                </p>
                            </div>
                            @if($voucherDiscountAmount > 0)
                                <p class="text-lg font-bold text-green-700 dark:text-green-300">
                                    💰 Discount: KES {{ number_format($voucherDiscountAmount, 2) }}
                                </p>
                            @endif
                        </div>
                    @endif
                    
                    <div class="flex space-x-3">
                        <button wire:click="applyVoucher" 
                                class="flex-1 bg-gradient-to-r from-green-500 to-emerald-600 hover:from-green-600 hover:to-emerald-700 text-white font-semibold py-3 px-6 rounded-xl transition-all duration-200 transform hover:scale-105">
                            <x-heroicon-o-check class="w-5 h-5 mr-2 inline" />
                            Apply Voucher
                        </button>
                        
                        @if($appliedVoucherCode)
                            <button wire:click="removeVoucher" 
                                    class="bg-red-100 hover:bg-red-200 text-red-700 font-medium py-3 px-6 rounded-xl transition-colors">
                                <x-heroicon-o-trash class="w-5 h-5 mr-2 inline" />
                                Remove
                            </button>
                        @endif
                    </div>
                </div>
            </div>
        </div>
        @endif

        <!-- Discount Coupon Modal -->
        @if($showCouponModal)
        <div x-data="{ show: @entangle('showCouponModal') }"
             x-show="show" 
             x-cloak
             x-transition:enter="transition ease-out duration-300"
             x-transition:enter-start="opacity-0 scale-95"
             x-transition:enter-end="opacity-100 scale-100"
             x-transition:leave="transition ease-in duration-200"
             x-transition:leave-start="opacity-100 scale-100"
             x-transition:leave-end="opacity-0 scale-95"
             class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50">
            
            <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl w-full max-w-md">
                <!-- Modal Header -->
                <div class="bg-gradient-to-r from-orange-500 to-red-500 text-white p-6 rounded-t-2xl">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-3">
                            <div class="w-10 h-10 bg-white bg-opacity-20 rounded-full flex items-center justify-center">
                                <x-heroicon-o-ticket class="w-6 h-6" />
                            </div>
                            <div>
                                <h3 class="text-xl font-bold">Discount Coupon</h3>
                                <p class="text-orange-100 text-sm">Save with your coupon code</p>
                            </div>
                        </div>
                        <button wire:click="$set('showCouponModal', false)" 
                                class="text-white hover:bg-white hover:bg-opacity-20 rounded-lg p-2 transition-colors">
                            <x-heroicon-o-x-mark class="w-5 h-5" />
                        </button>
                    </div>
                </div>
                
                <!-- Modal Content -->
                <div class="p-6 space-y-6">
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-3">Enter Coupon Code</label>
                        <div class="relative">
                            <input wire:model="appliedCouponCode" 
                                   type="text" 
                                   placeholder="e.g., SAVE20"
                                   class="w-full px-4 py-3 pl-12 border-2 border-gray-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-orange-500 focus:border-transparent bg-gray-50 dark:bg-gray-700 text-lg font-mono uppercase">
                            <x-heroicon-o-tag class="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
                        </div>
                    </div>
                    
                    @if($appliedCouponCode)
                        <div class="bg-gradient-to-r from-orange-50 to-red-50 dark:from-orange-900/20 dark:to-red-900/20 p-4 rounded-xl border border-orange-200 dark:border-orange-800">
                            <div class="flex items-center space-x-2 mb-2">
                                <x-heroicon-o-check-circle class="w-5 h-5 text-orange-600" />
                                <p class="text-sm font-medium text-orange-800 dark:text-orange-200">
                                    Coupon: <span class="font-mono">{{ $appliedCouponCode }}</span>
                                </p>
                            </div>
                            @if($couponDiscountAmount > 0)
                                <p class="text-lg font-bold text-orange-700 dark:text-orange-300">
                                    🎯 Discount: KES {{ number_format($couponDiscountAmount, 2) }}
                                </p>
                            @endif
                        </div>
                    @endif
                    
                    <div class="flex space-x-3">
                        <button wire:click="applyCoupon" 
                                class="flex-1 bg-gradient-to-r from-orange-500 to-red-500 hover:from-orange-600 hover:to-red-600 text-white font-semibold py-3 px-6 rounded-xl transition-all duration-200 transform hover:scale-105">
                            <x-heroicon-o-check class="w-5 h-5 mr-2 inline" />
                            Apply Coupon
                        </button>
                        
                        @if($appliedCouponCode)
                            <button wire:click="removeCoupon" 
                                    class="bg-red-100 hover:bg-red-200 text-red-700 font-medium py-3 px-6 rounded-xl transition-colors">
                                <x-heroicon-o-trash class="w-5 h-5 mr-2 inline" />
                                Remove
                            </button>
                        @endif
                    </div>
                </div>
            </div>
        </div>
        @endif

        <!-- Loyalty Points Modal -->
        @if($showLoyaltyModal)
        <div x-data="{ show: @entangle('showLoyaltyModal') }"
             x-show="show" 
             x-cloak
             x-transition:enter="transition ease-out duration-300"
             x-transition:enter-start="opacity-0 scale-95"
             x-transition:enter-end="opacity-100 scale-100"
             x-transition:leave="transition ease-in duration-200"
             x-transition:leave-start="opacity-100 scale-100"
             x-transition:leave-end="opacity-0 scale-95"
             class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50">
            
            <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl w-full max-w-md">
                <!-- Modal Header -->
                <div class="bg-gradient-to-r from-yellow-500 to-amber-500 text-white p-6 rounded-t-2xl">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-3">
                            <div class="w-10 h-10 bg-white bg-opacity-20 rounded-full flex items-center justify-center">
                                <x-heroicon-o-star class="w-6 h-6" />
                            </div>
                            <div>
                                <h3 class="text-xl font-bold">Loyalty Points</h3>
                                <p class="text-yellow-100 text-sm">Redeem your rewards</p>
                            </div>
                        </div>
                        <button wire:click="$set('showLoyaltyModal', false)" 
                                class="text-white hover:bg-white hover:bg-opacity-20 rounded-lg p-2 transition-colors">
                            <x-heroicon-o-x-mark class="w-5 h-5" />
                        </button>
                    </div>
                </div>
                
                <!-- Modal Content -->
                <div class="p-6 space-y-6">
                    <div class="bg-gradient-to-r from-yellow-50 to-amber-50 dark:from-yellow-900/20 dark:to-amber-900/20 p-4 rounded-xl border border-yellow-200 dark:border-yellow-800">
                        <div class="flex items-center justify-between mb-2">
                            <div class="flex items-center space-x-2">
                                <x-heroicon-o-star class="w-5 h-5 text-yellow-600" />
                                <span class="text-sm font-medium text-yellow-800 dark:text-yellow-200">Available Points</span>
                            </div>
                            <span class="text-2xl font-bold text-yellow-700 dark:text-yellow-300">{{ number_format($customerLoyaltyPoints) }}</span>
                        </div>
                        <p class="text-xs text-yellow-600 dark:text-yellow-400">
                            💰 1 point = KES 1.00
                        </p>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-3">Points to Use</label>
                        <div class="relative">
                            <input wire:model="loyaltyPointsToUse" 
                                   type="number" 
                                   min="0"
                                   max="{{ $customerLoyaltyPoints }}"
                                   placeholder="Enter points"
                                   class="w-full px-4 py-3 pl-12 border-2 border-gray-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-yellow-500 focus:border-transparent bg-gray-50 dark:bg-gray-700 text-lg">
                            <x-heroicon-o-star class="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
                        </div>
                    </div>
                    
                    @if($loyaltyPointsToUse > 0)
                        <div class="bg-gradient-to-r from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 p-4 rounded-xl border border-green-200 dark:border-green-800">
                            <div class="flex items-center justify-between">
                                <span class="text-sm font-medium text-green-800 dark:text-green-200">Your Discount</span>
                                <span class="text-lg font-bold text-green-700 dark:text-green-300">
                                    KES {{ number_format($loyaltyPointsToUse, 2) }}
                                </span>
                            </div>
                        </div>
                    @endif
                    
                    <div class="flex space-x-3">
                        <button wire:click="updateLoyaltyPoints" 
                                class="flex-1 bg-gradient-to-r from-yellow-500 to-amber-500 hover:from-yellow-600 hover:to-amber-600 text-white font-semibold py-3 px-6 rounded-xl transition-all duration-200 transform hover:scale-105">
                            <x-heroicon-o-check class="w-5 h-5 mr-2 inline" />
                            Use Points
                        </button>
                        
                        @if($loyaltyPointsToUse > 0)
                            <button wire:click="clearLoyaltyPoints" 
                                    class="bg-red-100 hover:bg-red-200 text-red-700 font-medium py-3 px-6 rounded-xl transition-colors">
                                <x-heroicon-o-trash class="w-5 h-5 mr-2 inline" />
                                Clear
                            </button>
                        @endif
                    </div>
                </div>
            </div>
        </div>
        @endif

        <!-- Split Payment Modal -->
        @if($showPaymentSplitModal)
        <div x-data="{ show: @entangle('showPaymentSplitModal') }"
             x-show="show" 
             x-cloak
             x-transition:enter="transition ease-out duration-300"
             x-transition:enter-start="opacity-0 scale-95"
             x-transition:enter-end="opacity-100 scale-100"
             x-transition:leave="transition ease-in duration-200"
             x-transition:leave-start="opacity-100 scale-100"
             x-transition:leave-end="opacity-0 scale-95"
             class="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full z-50">
            
            <div class="relative top-10 mx-auto p-6 border w-[600px] max-h-[90vh] overflow-y-auto shadow-lg rounded-xl bg-white dark:bg-gray-800">
                <div class="space-y-6">
                    <!-- Header -->
                    <div class="flex items-center justify-between pb-4 border-b border-gray-200 dark:border-gray-700">
                        <div>
                            <h3 class="text-xl font-semibold text-gray-900 dark:text-white">Split Payment</h3>
                            <p class="text-sm text-gray-500 dark:text-gray-400">Divide payment across multiple methods</p>
                        </div>
                        <button wire:click="$set('showPaymentSplitModal', false)" 
                                class="text-gray-400 hover:text-gray-600 dark:hover:text-gray-300 rounded-lg p-1 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors">
                            <x-heroicon-o-x-mark class="w-6 h-6" />
                        </button>
                    </div>
                    
                    <!-- Total Amount -->
                    <div class="bg-gradient-to-r from-blue-50 to-indigo-50 dark:from-blue-900/20 dark:to-indigo-900/20 p-4 rounded-lg border border-blue-200 dark:border-blue-800">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-sm font-medium text-blue-800 dark:text-blue-200">Total Amount to Split</p>
                                <p class="text-2xl font-bold text-blue-900 dark:text-blue-100">KES {{ number_format($totalAmount, 2) }}</p>
                            </div>
                            <div class="w-12 h-12 bg-blue-500 rounded-full flex items-center justify-center">
                                <x-heroicon-o-credit-card class="w-6 h-6 text-white" />
                            </div>
                        </div>
                    </div>
                    
                    <!-- Payment Splits -->
                    <div class="space-y-4">
                        <div class="flex items-center justify-between">
                            <h4 class="text-lg font-medium text-gray-900 dark:text-white">Payment Methods</h4>
                            <span class="text-sm text-gray-500 dark:text-gray-400">{{ count($paymentSplits) }} method(s)</span>
                        </div>
                        
                        <div class="space-y-3 max-h-80 overflow-y-auto">
                            @foreach($paymentSplits as $index => $split)
                                <div class="bg-gray-50 dark:bg-gray-700 rounded-lg p-4 border border-gray-200 dark:border-gray-600">
                                    <div class="flex items-center space-x-4">
                                        <!-- Payment Method Selector -->
                                        <div class="flex-shrink-0">
                                            <label class="block text-xs font-medium text-gray-700 dark:text-gray-300 mb-1">Method</label>
                                            <select wire:model="paymentSplits.{{ $index }}.payment_method" 
                                                    class="w-32 text-sm border border-gray-300 dark:border-gray-600 rounded-lg px-3 py-2 bg-white dark:bg-gray-800 focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                                                <option value="cash">💵 Cash</option>
                                                <option value="mpesa">📱 M-Pesa</option>
                                                <option value="card">💳 Card</option>
                                                <option value="bank_transfer">🏦 Bank Transfer</option>
                                                <option value="gift_voucher">🎁 Gift Voucher</option>
                                            </select>
                                        </div>
                                        
                                        <!-- Amount Input -->
                                        <div class="flex-1">
                                            <label class="block text-xs font-medium text-gray-700 dark:text-gray-300 mb-1">Amount (KES)</label>
                                            <div class="relative">
                                                <span class="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-500 text-sm">KES</span>
                                                <input wire:model="paymentSplits.{{ $index }}.amount" 
                                                       type="number" 
                                                       step="0.01"
                                                       min="0"
                                                       placeholder="0.00"
                                                       class="w-full pl-12 pr-4 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-800 focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                                            </div>
                                        </div>
                                        
                                        <!-- Reference Number (for non-cash) -->
                                        @if(isset($split['payment_method']) && $split['payment_method'] !== 'cash')
                                            <div class="flex-1">
                                                <label class="block text-xs font-medium text-gray-700 dark:text-gray-300 mb-1">Reference</label>
                                                <input wire:model="paymentSplits.{{ $index }}.reference" 
                                                       type="text" 
                                                       placeholder="Transaction ref"
                                                       class="w-full px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-800 focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                                            </div>
                                        @endif
                                        
                                        <!-- Remove Button -->
                                        @if(count($paymentSplits) > 1)
                                            <div class="flex-shrink-0">
                                                <label class="block text-xs text-transparent mb-1">Remove</label>
                                                <button wire:click="removePaymentSplit({{ $index }})" 
                                                        class="w-10 h-10 text-red-500 hover:text-red-600 hover:bg-red-50 dark:hover:bg-red-900/20 rounded-lg transition-colors flex items-center justify-center">
                                                    <x-heroicon-o-trash class="w-5 h-5" />
                                                </button>
                                            </div>
                                        @endif
                                    </div>
                                    
                                    <!-- Payment method specific fields -->
                                    @if(isset($split['payment_method']))
                                        @if($split['payment_method'] === 'mpesa')
                                            <div class="mt-3 pt-3 border-t border-gray-200 dark:border-gray-600">
                                                <input wire:model="paymentSplits.{{ $index }}.phone_number" 
                                                       type="tel" 
                                                       placeholder="254XXXXXXXXX"
                                                       class="w-full px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-800 focus:ring-2 focus:ring-blue-500">
                                            </div>
                                        @elseif($split['payment_method'] === 'card')
                                            <div class="mt-3 pt-3 border-t border-gray-200 dark:border-gray-600 grid grid-cols-2 gap-3">
                                                <input wire:model="paymentSplits.{{ $index }}.card_number" 
                                                       type="text" 
                                                       placeholder="Card Number"
                                                       class="px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-800 focus:ring-2 focus:ring-blue-500">
                                                <input wire:model="paymentSplits.{{ $index }}.cardholder_name" 
                                                       type="text" 
                                                       placeholder="Cardholder Name"
                                                       class="px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-800 focus:ring-2 focus:ring-blue-500">
                                            </div>
                                        @endif
                                    @endif
                                </div>
                            @endforeach
                        </div>
                        
                        <!-- Add Payment Method Button -->
                        <button wire:click="addPaymentSplit" 
                                class="w-full flex items-center justify-center py-3 border-2 border-dashed border-gray-300 dark:border-gray-600 hover:border-gray-400 dark:hover:border-gray-500 text-gray-600 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300 rounded-lg transition-colors">
                            <x-heroicon-o-plus class="w-5 h-5 mr-2" />
                            <span class="font-medium">Add Another Payment Method</span>
                        </button>
                    </div>
                    
                    <!-- Summary and Actions -->
                    <div class="pt-4 border-t border-gray-200 dark:border-gray-700">
                        <div class="flex items-center justify-between mb-4">
                            <div class="space-y-1">
                                <p class="text-sm text-gray-600 dark:text-gray-400">Split Total</p>
                                <p class="text-lg font-semibold text-gray-900 dark:text-white">
                                    KES {{ number_format(array_sum(array_column($paymentSplits, 'amount')), 2) }}
                                </p>
                            </div>
                            
                            <div class="space-y-1 text-right">
                                <p class="text-sm text-gray-600 dark:text-gray-400">Remaining</p>
                                <p class="text-lg font-semibold {{ (array_sum(array_column($paymentSplits, 'amount')) >= $totalAmount) ? 'text-green-600' : 'text-red-600' }}">
                                    KES {{ number_format($totalAmount - array_sum(array_column($paymentSplits, 'amount')), 2) }}
                                </p>
                            </div>
                        </div>
                        
                        <!-- Action Buttons -->
                        <div class="flex space-x-3">
                            <button wire:click="$set('showPaymentSplitModal', false)" 
                                    class="flex-1 bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 font-medium py-3 px-4 rounded-lg transition-colors">
                                Cancel
                            </button>
                            
                            <button wire:click="processSplitPayment" 
                                    wire:loading.attr="disabled"
                                    wire:target="processSplitPayment"
                                    {{ (array_sum(array_column($paymentSplits, 'amount')) != $totalAmount) ? 'disabled' : '' }}
                                    class="flex-1 bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 disabled:from-gray-400 disabled:to-gray-500 text-white font-semibold py-3 px-4 rounded-lg transition-all duration-200 disabled:cursor-not-allowed">
                                
                                <span wire:loading.remove wire:target="processSplitPayment">
                                    <x-heroicon-o-credit-card class="w-5 h-5 mr-2 inline" />
                                    Process Split Payment
                                </span>
                                
                                <span wire:loading wire:target="processSplitPayment" class="flex items-center justify-center">
                                    <svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                                    </svg>
                                    Processing...
                                </span>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        @endif

        <!-- Professional Service Booking Modal -->
        @if($showServiceBookingFlow)
        <div x-data="{ show: @entangle('showServiceBookingFlow') }"
             x-show="show" 
             x-cloak
             x-transition:enter="transition ease-out duration-300"
             x-transition:enter-start="opacity-0 scale-95"
             x-transition:enter-end="opacity-100 scale-100"
             x-transition:leave="transition ease-in duration-200"
             x-transition:leave-start="opacity-100 scale-100"
             x-transition:leave-end="opacity-0 scale-95"
             class="fixed inset-0 bg-black bg-opacity-60 backdrop-blur-sm flex items-center justify-center p-4 z-50">
            
            <div class="bg-white dark:bg-gray-800 rounded-3xl shadow-2xl border border-gray-200 dark:border-gray-700 w-full max-w-4xl max-h-[95vh] overflow-hidden flex flex-col">
                @php $bookingItem = $this->getServiceBookingItem(); @endphp
                
                @if($bookingItem)
                    <!-- Modern Header with Service Info -->
                    <div class="bg-gradient-to-r from-blue-600 via-purple-600 to-blue-700 text-white p-8 relative overflow-hidden">
                        <div class="absolute inset-0 bg-gradient-to-r from-blue-500/20 to-purple-500/20"></div>
                        
                        <div class="relative flex items-center justify-between">
                            <div class="flex items-center space-x-4">
                                <div class="w-16 h-16 bg-white/20 backdrop-blur-sm rounded-2xl flex items-center justify-center border border-white/30">
                                    <x-heroicon-o-sparkles class="w-8 h-8 text-white" />
                                </div>
                                <div>
                                    <h3 class="text-2xl font-bold mb-1">{{ $bookingItem['name'] }}</h3>
                                    <div class="flex items-center space-x-4 text-blue-100">
                                        <span class="flex items-center">
                                            <x-heroicon-o-clock class="w-4 h-4 mr-1" />
                                            {{ $bookingItem['duration'] ?? 30 }}min
                                        </span>
                                        <span class="flex items-center">
                                            <x-heroicon-o-currency-dollar class="w-4 h-4 mr-1" />
                                            KES {{ number_format($bookingItem['price']) }}
                                        </span>
                                    </div>
                                </div>
                            </div>
                            <button wire:click="cancelServiceBooking" 
                                    class="text-white/80 hover:text-white hover:bg-white/20 rounded-xl p-3 transition-all duration-200 backdrop-blur-sm">
                                <x-heroicon-o-x-mark class="w-6 h-6" />
                            </button>
                        </div>
                        
                        <!-- Professional Progress Steps -->
                        <div class="mt-8 bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/20">
                            <div class="flex items-center justify-between">
                                <!-- Step 1: Staff Selection -->
                                <div class="flex flex-col items-center flex-1">
                                    <div class="relative">
                                        <div class="w-12 h-12 {{ $currentBookingStep === 'staff' ? 'bg-white text-blue-600 ring-4 ring-white/30' : (in_array($currentBookingStep, ['datetime', 'customer', 'payment']) ? 'bg-green-500 text-white' : 'bg-white/20 text-white border-2 border-white/40') }} rounded-full flex items-center justify-center text-sm font-bold shadow-lg transition-all duration-300">
                                            @if(in_array($currentBookingStep, ['datetime', 'customer', 'payment']))
                                                <x-heroicon-o-check class="w-6 h-6" />
                                            @else
                                                <x-heroicon-o-user class="w-6 h-6" />
                                            @endif
                                        </div>
                                        @if(in_array($currentBookingStep, ['datetime', 'customer', 'payment']))
                                            <div class="absolute -top-1 -right-1 w-4 h-4 bg-green-400 rounded-full border-2 border-white"></div>
                                        @endif
                                    </div>
                                    <span class="text-sm font-semibold mt-3 {{ $currentBookingStep === 'staff' ? 'text-white' : 'text-blue-200' }}">Select Staff</span>
                                </div>
                                
                                <!-- Progress Line 1 -->
                                <div class="flex-1 h-2 mx-4 {{ in_array($currentBookingStep, ['datetime', 'customer', 'payment']) ? 'bg-green-400' : 'bg-white/30' }} rounded-full transition-all duration-500"></div>
                                
                                <!-- Step 2: Date & Time -->
                                <div class="flex flex-col items-center flex-1">
                                    <div class="relative">
                                        <div class="w-12 h-12 {{ $currentBookingStep === 'datetime' ? 'bg-white text-blue-600 ring-4 ring-white/30' : (in_array($currentBookingStep, ['customer', 'payment']) ? 'bg-green-500 text-white' : 'bg-white/20 text-white border-2 border-white/40') }} rounded-full flex items-center justify-center text-sm font-bold shadow-lg transition-all duration-300">
                                            @if(in_array($currentBookingStep, ['customer', 'payment']))
                                                <x-heroicon-o-check class="w-6 h-6" />
                                            @else
                                                <x-heroicon-o-calendar-days class="w-6 h-6" />
                                            @endif
                                        </div>
                                        @if(in_array($currentBookingStep, ['customer', 'payment']))
                                            <div class="absolute -top-1 -right-1 w-4 h-4 bg-green-400 rounded-full border-2 border-white"></div>
                                        @endif
                                    </div>
                                    <span class="text-sm font-semibold mt-3 {{ $currentBookingStep === 'datetime' ? 'text-white' : 'text-blue-200' }}">Date & Time</span>
                                </div>
                                
                                <!-- Progress Line 2 -->
                                <div class="flex-1 h-2 mx-4 {{ in_array($currentBookingStep, ['customer', 'payment']) ? 'bg-green-400' : 'bg-white/30' }} rounded-full transition-all duration-500"></div>
                                
                                <!-- Step 3: Customer Details -->
                                <div class="flex flex-col items-center flex-1">
                                    <div class="relative">
                                        <div class="w-12 h-12 {{ $currentBookingStep === 'customer' ? 'bg-white text-blue-600 ring-4 ring-white/30' : ($currentBookingStep === 'payment' ? 'bg-green-500 text-white' : 'bg-white/20 text-white border-2 border-white/40') }} rounded-full flex items-center justify-center text-sm font-bold shadow-lg transition-all duration-300">
                                            @if($currentBookingStep === 'payment')
                                                <x-heroicon-o-check class="w-6 h-6" />
                                            @else
                                                <x-heroicon-o-user-plus class="w-6 h-6" />
                                            @endif
                                        </div>
                                        @if($currentBookingStep === 'payment')
                                            <div class="absolute -top-1 -right-1 w-4 h-4 bg-green-400 rounded-full border-2 border-white"></div>
                                        @endif
                                    </div>
                                    <span class="text-sm font-semibold mt-3 {{ $currentBookingStep === 'customer' ? 'text-white' : 'text-blue-200' }}">Customer Info</span>
                                </div>
                                
                                <!-- Progress Line 3 -->
                                <div class="flex-1 h-2 mx-4 {{ $currentBookingStep === 'payment' ? 'bg-green-400' : 'bg-white/30' }} rounded-full transition-all duration-500"></div>
                                
                                <!-- Step 4: Complete -->
                                <div class="flex flex-col items-center flex-1">
                                    <div class="w-12 h-12 {{ $currentBookingStep === 'payment' ? 'bg-white text-blue-600 ring-4 ring-white/30' : 'bg-white/20 text-white border-2 border-white/40' }} rounded-full flex items-center justify-center text-sm font-bold shadow-lg transition-all duration-300">
                                        <x-heroicon-o-check-circle class="w-6 h-6" />
                                    </div>
                                    <span class="text-sm font-semibold mt-3 {{ $currentBookingStep === 'payment' ? 'text-white' : 'text-blue-200' }}">Complete</span>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Dynamic Content Area -->
                    <div class="flex-1 overflow-y-auto">
                        <div class="p-8">
                            @if($currentBookingStep === 'staff')
                                @include('filament.pages.pos-booking.staff-selection')
                            @elseif($currentBookingStep === 'datetime')
                                @include('filament.pages.pos-booking.datetime-selection')
                            @elseif($currentBookingStep === 'customer')
                                @include('filament.pages.pos-booking.customer-details')
                            @elseif($currentBookingStep === 'payment')
                                @include('filament.pages.pos-booking.booking-summary')
                            @endif
                        </div>
                    </div>
                    
                    <!-- Professional Footer Navigation -->
                    <div class="bg-gray-50 dark:bg-gray-800 border-t border-gray-200 dark:border-gray-700 p-6">
                        <div class="flex justify-between items-center">
                            @if($currentBookingStep === 'staff')
                                <button wire:click="cancelServiceBooking" 
                                        class="flex items-center space-x-2 px-6 py-3 bg-white dark:bg-gray-700 border-2 border-gray-300 dark:border-gray-600 hover:border-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 text-gray-700 dark:text-gray-300 hover:text-red-700 dark:hover:text-red-300 font-semibold rounded-xl transition-all duration-200 shadow-sm hover:shadow-md">
                                    <x-heroicon-o-x-mark class="w-5 h-5" />
                                    <span>Cancel Booking</span>
                                </button>
                                <div class="flex items-center space-x-2 text-sm text-blue-600 dark:text-blue-400 font-medium bg-blue-50 dark:bg-blue-900/30 px-4 py-2 rounded-xl">
                                    <x-heroicon-o-information-circle class="w-4 h-4" />
                                    <span>Select a staff member to continue</span>
                                </div>
                            @elseif($currentBookingStep === 'datetime')
                                <button wire:click="goBackBookingStep" 
                                        class="flex items-center space-x-2 px-6 py-3 bg-white dark:bg-gray-700 border-2 border-gray-300 dark:border-gray-600 hover:border-blue-400 hover:bg-blue-50 dark:hover:bg-blue-900/20 text-gray-700 dark:text-gray-300 hover:text-blue-700 dark:hover:text-blue-300 font-semibold rounded-xl transition-all duration-200 shadow-sm hover:shadow-md">
                                    <x-heroicon-o-arrow-left class="w-5 h-5" />
                                    <span>Back to Staff</span>
                                </button>
                                <div class="flex items-center space-x-2 text-sm text-blue-600 dark:text-blue-400 font-medium bg-blue-50 dark:bg-blue-900/30 px-4 py-2 rounded-xl">
                                    <x-heroicon-o-information-circle class="w-4 h-4" />
                                    <span>Select your preferred date and time</span>
                                </div>
                            @elseif($currentBookingStep === 'customer')
                                <button wire:click="goBackBookingStep" 
                                        class="flex items-center space-x-2 px-6 py-3 bg-white dark:bg-gray-700 border-2 border-gray-300 dark:border-gray-600 hover:border-blue-400 hover:bg-blue-50 dark:hover:bg-blue-900/20 text-gray-700 dark:text-gray-300 hover:text-blue-700 dark:hover:text-blue-300 font-semibold rounded-xl transition-all duration-200 shadow-sm hover:shadow-md">
                                    <x-heroicon-o-arrow-left class="w-5 h-5" />
                                    <span>Back to Date & Time</span>
                                </button>
                                @if(isset($selectedCustomer) || ($selectedCustomerType === 'new' && !empty($customerData['name']) && !empty($customerData['phone'])))
                                    <button wire:click="proceedToPayment" 
                                            class="flex items-center space-x-2 px-8 py-3 bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-bold rounded-xl transition-all duration-200 transform hover:scale-105 shadow-lg hover:shadow-xl">
                                        <span>Continue to Summary</span>
                                        <x-heroicon-o-arrow-right class="w-5 h-5" />
                                    </button>
                                @else
                                    <div class="flex items-center space-x-2 text-sm text-orange-600 dark:text-orange-400 font-medium bg-orange-50 dark:bg-orange-900/30 px-4 py-2 rounded-xl">
                                        <x-heroicon-o-exclamation-triangle class="w-4 h-4" />
                                        <span>
                                            @if($selectedCustomerType === 'new')
                                                Complete required customer details to continue
                                            @else
                                                Please select or search for a customer
                                            @endif
                                        </span>
                                    </div>
                                @endif
                            @elseif($currentBookingStep === 'payment')
                                <button wire:click="goBackBookingStep" 
                                        class="flex items-center space-x-2 px-6 py-3 bg-white dark:bg-gray-700 border-2 border-gray-300 dark:border-gray-600 hover:border-blue-400 hover:bg-blue-50 dark:hover:bg-blue-900/20 text-gray-700 dark:text-gray-300 hover:text-blue-700 dark:hover:text-blue-300 font-semibold rounded-xl transition-all duration-200 shadow-sm hover:shadow-md">
                                    <x-heroicon-o-arrow-left class="w-5 h-5" />
                                    <span>Back to Customer</span>
                                </button>
                                <button wire:click="completeServiceBooking" 
                                        class="flex items-center space-x-3 px-8 py-3 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-bold rounded-xl transition-all duration-200 transform hover:scale-105 shadow-lg hover:shadow-xl">
                                    <x-heroicon-o-shopping-cart class="w-5 h-5" />
                                    <span>Add to Cart & Continue</span>
                                </button>
                            @endif
                        </div>
                    </div>
                @endif
            </div>
        </div>
        @endif

        <!-- Booking Confirmation Modal -->
        @if($showBookingConfirmation)
        <div x-data="{ show: @entangle('showBookingConfirmation') }"
             x-show="show" 
             x-cloak
             x-transition:enter="transition ease-out duration-300"
             x-transition:enter-start="opacity-0 scale-95"
             x-transition:enter-end="opacity-100 scale-100"
             x-transition:leave="transition ease-in duration-200"
             x-transition:leave-start="opacity-100 scale-100"
             x-transition:leave-end="opacity-0 scale-95"
             class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50">
            
            <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl border-2 border-green-200 dark:border-green-600 w-full max-w-md overflow-hidden">
                <!-- Success Header -->
                <div class="bg-gradient-to-r from-green-500 to-emerald-600 text-white p-6 text-center">
                    <div class="w-16 h-16 bg-white bg-opacity-20 rounded-full flex items-center justify-center mx-auto mb-4">
                        <x-heroicon-o-check-circle class="w-10 h-10 text-white" />
                    </div>
                    <h3 class="text-2xl font-bold mb-2">Booking Confirmed!</h3>
                    <p class="text-green-100">Your appointment has been successfully scheduled</p>
                </div>
                
                <!-- Booking Details -->
                <div class="p-6">
                    @if(isset($confirmedBooking))
                        <div class="space-y-4 mb-6">
                            <!-- Service Info -->
                            <div class="flex items-center p-3 bg-blue-50 dark:bg-blue-900/20 rounded-lg border border-blue-200 dark:border-blue-700">
                                <div class="w-10 h-10 bg-blue-500 rounded-lg flex items-center justify-center mr-3">
                                    <x-heroicon-o-sparkles class="w-5 h-5 text-white" />
                                </div>
                                <div>
                                    <p class="font-semibold text-gray-900 dark:text-white">{{ $confirmedBooking['service_name'] }}</p>
                                    <p class="text-sm text-blue-600 dark:text-blue-400">KES {{ number_format($confirmedBooking['amount']) }}</p>
                                </div>
                            </div>
                            
                            <!-- Staff Info -->
                            @if(!empty($confirmedBooking['staff_name']))
                            <div class="flex items-center p-3 bg-green-50 dark:bg-green-900/20 rounded-lg border border-green-200 dark:border-green-700">
                                <div class="w-10 h-10 bg-green-500 rounded-lg flex items-center justify-center mr-3">
                                    <x-heroicon-o-user class="w-5 h-5 text-white" />
                                </div>
                                <div>
                                    <p class="font-semibold text-gray-900 dark:text-white">{{ $confirmedBooking['staff_name'] }}</p>
                                    <p class="text-sm text-green-600 dark:text-green-400">Your therapist</p>
                                </div>
                            </div>
                            @endif
                            
                            <!-- DateTime Info -->
                            @if(!empty($confirmedBooking['appointment_date']) && !empty($confirmedBooking['appointment_time']))
                            <div class="flex items-center p-3 bg-purple-50 dark:bg-purple-900/20 rounded-lg border border-purple-200 dark:border-purple-700">
                                <div class="w-10 h-10 bg-purple-500 rounded-lg flex items-center justify-center mr-3">
                                    <x-heroicon-o-calendar-days class="w-5 h-5 text-white" />
                                </div>
                                <div>
                                    <p class="font-semibold text-gray-900 dark:text-white">
                                        {{ date('l, F j, Y', strtotime($confirmedBooking['appointment_date'])) }}
                                    </p>
                                    <p class="text-sm text-purple-600 dark:text-purple-400">
                                        {{ date('g:i A', strtotime($confirmedBooking['appointment_time'])) }}
                                    </p>
                                </div>
                            </div>
                            @endif
                            
                            <!-- Customer Info -->
                            @if(!empty($confirmedBooking['customer_name']))
                            <div class="flex items-center p-3 bg-orange-50 dark:bg-orange-900/20 rounded-lg border border-orange-200 dark:border-orange-700">
                                <div class="w-10 h-10 bg-orange-500 rounded-lg flex items-center justify-center mr-3">
                                    <x-heroicon-o-user-circle class="w-5 h-5 text-white" />
                                </div>
                                <div>
                                    <p class="font-semibold text-gray-900 dark:text-white">{{ $confirmedBooking['customer_name'] }}</p>
                                    <p class="text-sm text-orange-600 dark:text-orange-400">{{ $confirmedBooking['customer_phone'] ?? $confirmedBooking['customer_email'] }}</p>
                                </div>
                            </div>
                            @endif
                            
                            <!-- Booking Reference -->
                            <div class="p-3 bg-gray-50 dark:bg-gray-700 rounded-lg border border-gray-200 dark:border-gray-600 text-center">
                                <p class="text-xs text-gray-600 dark:text-gray-400 mb-1">Booking Reference</p>
                                <p class="font-bold text-lg text-gray-900 dark:text-white tracking-wider">{{ $confirmedBooking['reference'] ?? 'BK' . str_pad(rand(1, 9999), 4, '0', STR_PAD_LEFT) }}</p>
                            </div>
                        </div>
                    @endif
                    
                    <!-- Action Buttons -->
                    <div class="space-y-3">
                        <button wire:click="printReceipt" 
                                class="w-full bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 text-white font-bold py-3 px-4 rounded-lg transition-all duration-200">
                            <x-heroicon-o-printer class="w-5 h-5 mr-2 inline" />
                            Print Receipt
                        </button>
                        
                        <div class="grid grid-cols-2 gap-3">
                            <button wire:click="sendSMSConfirmation" 
                                    class="py-2 px-3 bg-green-100 dark:bg-green-900/30 hover:bg-green-200 dark:hover:bg-green-900/50 text-green-700 dark:text-green-300 font-medium text-sm rounded-lg transition-colors">
                                <x-heroicon-o-chat-bubble-left-ellipsis class="w-4 h-4 mr-1 inline" />
                                Send SMS
                            </button>
                            <button wire:click="sendEmailConfirmation" 
                                    class="py-2 px-3 bg-blue-100 dark:bg-blue-900/30 hover:bg-blue-200 dark:hover:bg-blue-900/50 text-blue-700 dark:text-blue-300 font-medium text-sm rounded-lg transition-colors">
                                <x-heroicon-o-envelope class="w-4 h-4 mr-1 inline" />
                                Send Email
                            </button>
                        </div>
                        
                        <button wire:click="closeBookingConfirmation" 
                                class="w-full py-2 px-4 bg-gray-200 dark:bg-gray-700 hover:bg-gray-300 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 font-medium rounded-lg transition-colors">
                            <x-heroicon-o-x-mark class="w-4 h-4 mr-1 inline" />
                            Close
                        </button>
                    </div>
                </div>
            </div>
        </div>
        @endif

        </div>
    </div>

    @push('styles')
    <style>
        [x-cloak] {
            display: none !important;
        }
        
        /* Enhanced modal backdrop */
        .modal-backdrop {
            backdrop-filter: blur(4px);
        }
    </style>
    @endpush

    @push('scripts')
    <script>
        function posTerminal() {
            return {
                init() {
                    this.setupKeyboardShortcuts();
                },
                
                setupKeyboardShortcuts() {
                    document.addEventListener('keydown', (e) => {
                        if (e.key === 'Escape') {
                            @this.set('searchTerm', '');
                        }
                    
                        if (e.key === 'Enter' && e.ctrlKey) {
                            @this.call('processPayment');
                        }
                    
                        // Quick access keys
                        if (e.key === 'v' && e.ctrlKey) {
                            e.preventDefault();
                            @this.set('showVoucherModal', true);
                        }
                        
                        if (e.key === 'c' && e.ctrlKey) {
                            e.preventDefault();
                            @this.set('showCouponModal', true);
                        }
                        
                        if (e.key === 'l' && e.ctrlKey) {
                            e.preventDefault();
                            @this.set('showLoyaltyModal', true);
                        }
                    });
                }
            }
        }

        // Enhanced payment processing
        document.addEventListener('livewire:initialized', () => {
            @this.on('mpesa-payment-initiated', (event) => {
                setTimeout(() => {
                    const success = Math.random() > 0.3;
                    
                    if (success) {
                        const mpesaId = 'MPX' + Math.random().toString(36).substr(2, 9).toUpperCase();
                        @this.call('handleMpesaSuccess', event.transaction_id, mpesaId);
                    } else {
                        @this.call('handleMpesaFailure', event.transaction_id, 'Customer cancelled payment');
                    }
                }, 3000);
            });

            @this.on('print-receipt', (event) => {
                console.log('Printing receipt for transaction:', event.transaction_id);
            });
        });
    </script>
    @endpush
</x-filament-panels::page>